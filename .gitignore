node_modules/
# dist/ is a committed GENERATED artefact since 2026-08-30 (pnpm dist:build) —
# the drift gate checks its bytes like docs/ and the token artefacts.
reports/
*.log
.DS_Store
# Session-local tooling (launch configs, parallel worktrees): never repository content.
# vendor-dist's clean-tree check counts untracked files, so this must be ignored.
.claude/
