/* GENERATED by `pnpm dist:build` (scripts/generate-dist.ts): the real
 * component sources, types stripped, JSX preserved (esbuild), exposed as
 * window.LL globals for a buildless host (React UMD + Babel standalone).
 * Do not edit; edit the component .tsx sources and regenerate. */

// ── Button (button) ──
function Button({
  variant = "primary",
  href,
  type = "button",
  onClick,
  children
}) {
  const className = `ll-button ll-button--${variant}`;
  if (href !== void 0) {
    return <a className={className} href={href} onClick={onClick}>
        {children}
      </a>;
  }
  return <button className={className} type={type} onClick={onClick}>
      {children}
    </button>;
}

window.LL = Object.assign(window.LL || {}, { Button });
